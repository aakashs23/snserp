[pytest]
# Only collect the real suite. The test_*.py scripts at the backend root are
# dead debug scripts (test_stats.py imports a SessionLocal that no longer
# exists) and would fail collection if pytest walked the whole tree.
testpaths = tests
