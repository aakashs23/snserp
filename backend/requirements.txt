# Pinned for reproducible builds. Regenerate versions with `pip freeze` after an
# intentional upgrade, and run `pip-audit` in CI to catch vulnerable releases.

# Web Framework
fastapi==0.138.0
uvicorn[standard]==0.49.0

# Database
sqlalchemy[asyncio]==2.0.51
asyncpg==0.31.0
alembic==1.18.4

# Validation & Config
pydantic==2.13.4
pydantic-settings==2.14.2
email-validator==2.3.0  # required at runtime by pydantic's EmailStr

# Authentication
# (python-jose removed — it was unused and carries algorithm-confusion CVEs.
#  Token verification is delegated to Supabase Auth, see app/auth/supabase_jwt.py.)
passlib[bcrypt]==1.7.4

# Supabase
supabase==2.31.0

# File Processing
python-multipart==0.0.32
reportlab==5.0.0
pandas==3.0.3
openpyxl==3.1.5
num2words==0.5.14

# AI (shared with ai/ service)
langchain==1.3.10
langchain-ollama==1.1.0
langchain-community==0.4.2
chromadb==1.5.9
pymupdf==1.27.2.3
google-genai==2.10.0          # Gemini provider (was imported but unpinned/missing)
sentence-transformers==5.6.0  # cross-encoder reranking in rag_service

# OCR
paddleocr==3.7.0
paddlepaddle==3.3.1

# Utilities
python-dotenv==1.2.2
httpx==0.28.1
slowapi==0.1.10
tenacity==9.1.4
